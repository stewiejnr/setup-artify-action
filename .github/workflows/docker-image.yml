name: Test custom action

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/setup-artify-action@v1
      with:
        private-token: ${{ secrets.PRIVATE_TOKEN }}
    - name: Test Action
      run: echo This works
